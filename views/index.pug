extends layout

block content
  .container-xxl
  h1= title
  p Welcome to #{title}
  ul 
    each val in items
      li
        a(href=val.url)= val.name 
    else 
      p There are no items in inventory. Stock now!
