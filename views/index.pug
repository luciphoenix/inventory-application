extends layout

block content
  h1= title
  p Welcome to #{title}
  ul 
    each val in items
      li
        a(href=val.url)= val.name 
    else 
      p There are no items in inventory. Stock now!
